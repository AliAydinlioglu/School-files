 -- commentaar
 
 SELECT * = alle producten
 FROM products;
 
 SELECT prodctnaam, unitprice
 FROM products;
 
 select prodcutname, unitprice, categoryid
 from products
 
 <> =  -- verschillend cry + / om comentaar te schrijven of /* en einden met */
 
 order by = asc en desc om te sorteren
 
 select distinct = uniek namen
 
 select productid *unitstock as inventoryvalue
 
 select concat(left(firstname,1),left (lastname,4)) = aan elkaar steken
 
 select length productname;
 
 select ifnull(region, 'Niet Ingevuld')
 
 
 